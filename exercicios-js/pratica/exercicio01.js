/* ​ Crie uma função que dado dois valores (passados como parâmetros) mostre no console a soma, subtração,
multiplicação e divisão desses valores. */

function matematica(x, y) {
    let soma = console.log(`A Soma de ${x} + ${y} = ${x + y}`)
    let subtracao = console.log(`A Subtração de ${x} - ${y} = ${x - y}`)
    let multiplicao = console.log(`Multiplicação de ${x} x ${y} = ${x * y}`)
    let divisao = console.log(`A divisão de ${x} + ${y} = ${x / y}`)
}

matematica(10, 5)